import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#8ee361',
        justifyContent:'center',
        alignItems: 'center',
    },

    img:{
        width: 100,
        height: 100,
        marginTop: 20,
    },

    desc: {
        backgroundColor: '#58c62e',
        marginTop: 10,
        color: '#fff',
        width: 390,
        borderRadius: 6,
    }
}

)

export default styles;